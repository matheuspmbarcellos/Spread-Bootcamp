/* let lines = gets().split("\n");

let N = parseInt(lines.shift());
for (                         ) {
    if (           ) frase +=      ;
    else frase +=        ;
}

print(frase); */


let N = 5
const frase = []

for(let i = 0; i < N - 1; i++) {
    frase.push("Ho")
}
frase.push("Ho!")



console.log(frase.join(' '))
